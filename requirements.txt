# Web dashboard (productization layer) — install with: pip install -r requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
python-multipart>=0.0.9

# Market data (optional; only needed for the live Yahoo Finance provider).
# The dashboard runs fully on local deterministic fixtures without these.
pandas>=2.0
yfinance>=0.2.40

# Test runner
pytest>=8.0
